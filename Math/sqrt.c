
// Syntax : sqrt(number) : trả về căn bậc 2 của số đã chuoi

#include<stdio.h>
#include<math.h>

int main() {
    int a = 9;
    float b = 17;
    printf ("%f", sqrt((float)a));
    printf ("\n%.4f", sqrt(b));
    return 0;
}
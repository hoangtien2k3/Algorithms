
// Syntax : ceil(number) : làm tròn lên số đã cho. nó trả về số nguyên lớn hơn hoặc bằng số đã chuoi

#include<stdio.h>
#include<math.h>

int main() {
    float a = 3.6, b = 3.3;
    printf ("%.4f", ceil(a));
    printf ("\n%.4f", ceil(b));
    return 0;
}
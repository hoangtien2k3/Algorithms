
// Syntax: floor(number) : làm tròn xuống số đã cho. Nó trả về số nguyên nhỏ hơn hoặc bằng số đã cho

#include<stdio.h>
#include<math.h>

int main() {
    float a = 3.6, b = 3.2;
    printf ("%.4f", floor(a));
    printf ("\n%.4f", floor(b));
    return 0;
}